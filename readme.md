Библиотека для локализации на лету.

В структуру добавить тег и задать это имя при создании класса. 

```
translate := translate.New(stub, "TAG_NAME")
```

**stub** это "наследник" интерфейса Localizator

Пример в файле **main.go**